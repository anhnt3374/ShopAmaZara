# torch + torchvision are installed FIRST from the PyTorch cu130 index in the
# Dockerfile (their wheels bundle the CUDA 13 runtime), so they are NOT listed here.
fastapi==0.136.1
starlette==0.52.1
uvicorn[standard]==0.47.0
pydantic==2.13.4
httpx==0.28.1
pillow==12.2.0
transformers==4.57.6
tokenizers==0.22.2
huggingface_hub==0.36.2
safetensors==0.7.0
einops==0.8.2
sentencepiece==0.2.1
